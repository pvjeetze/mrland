# calcTradeExportShr

Calculate export shares of countries compared to total export. This is
based on export values from FAOSTAT. Function calculates this based on
average values of the specified years.

## Usage

``` r
calcTradeExportShr()
```

## Value

export shares as MAgPIE object

## See also

[`calcOutput`](https://rdrr.io/pkg/madrat/man/calcOutput.html),
[`calcFAOmassbalance`](https://rdrr.io/pkg/mrcommons/man/calcFAOmassbalance.html)

## Author

Ulrich Kreidenweis, Xiaoxi Wang

## Examples

``` r
if (FALSE) { # \dontrun{
a <- calcTradeExportShr()
} # }
```
